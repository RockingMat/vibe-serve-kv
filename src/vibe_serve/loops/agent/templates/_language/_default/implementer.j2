Use your language's standard build and dependency tooling. Initialize the project (manifest/lockfile) if it does not exist yet, declare new dependencies explicitly, and keep the build reproducible. Run the server and any scripts through that toolchain rather than assuming a global interpreter.
